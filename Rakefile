#!/usr/bin/env rake
require 'bundler/gem_tasks'
require 'adminlte-rails/source_file'
desc "Update with almasaeed2010's AdminLTE theme"
task 'update-adminlte' do
  files = SourceFile.new
  files.fetch
  #files.convert
  #files.cleanup
end